//STRING METHOD-
//strings are immutable in js
//these are build-in function to manipulate a string(block of code perform specific work)
let str="   Prathamesh Korde   ";
let newStr = str.toLowerCase();//str.toUpperCase();
console.log(str);//orignal string remain unchanged
console.log(newStr);
let whitespaces=str.trim();//remove whitespaces("space at starting and ending")
console.log(whitespaces);